<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="api">
    <insert id="batchInsertTick">
        INSERT ALL
        <foreach collection="list" item="item">
            INTO STOCK_REALTIME_TICK (
                TICK_ID,
                STOCK_CODE,
                CURRENT_PRICE,
                CHANGE_RATE,
                VOLUME,
                TRADE_TIME
            )
            VALUES (
                SEQ_STOCK_REALTIME_TICK.NEXTVAL,
                #{item.stockCode},
                #{item.currentPrice},
                #{item.changeRate},
                #{item.volume},
                #{item.tradeTime}
            )
        </foreach>
        SELECT * FROM dual
    </insert>
</mapper>